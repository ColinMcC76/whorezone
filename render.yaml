services:
  - type: web
    name: whorezone-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:api
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22
